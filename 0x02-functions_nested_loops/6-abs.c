#include "main.h"

/**
 * _abs - prints the absolute value of the number
 * @i : int data type
 * Return: return the absolute value
 */

int _abs(int i);
{
	if (i >= 0)
		return (i);
	else
		return (-1 * i);
}
