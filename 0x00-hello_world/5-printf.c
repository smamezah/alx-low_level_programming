#include <stdio.h>

/**
 * main - prints exactly an array of text with the printf function
 * Return: This returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
