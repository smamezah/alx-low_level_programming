#include <stdio.h>

/**
 * main - this code prints the alphabets in lowercasse
 * Return: this code returns 0
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
